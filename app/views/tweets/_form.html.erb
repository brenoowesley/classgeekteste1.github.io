<%= form_with(model: tweet, local: true) do |form| %>
  <% if tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
        <% tweet.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  
 <br>
  <div class="form-group">
    
    <%= form.text_area :conteudo, class: "form-control", placeholder:"Novo tweet" %> 
  </div>

  <%= form.label :photo, 'Imagem', class:'glyphicon glyphicon-camera' %>  
  <%= form.file_field :photo %> 

  <%= form.submit "Enviar", class: "btn btn-primary", style:'margin-left:14rem'%>

 

<% end %>
