<center>

	<div class="card" style="width: 40rem;">
		<% if @tweet.photo.file? %>
			<%= image_tag @tweet.photo.url, class:"card-img-top mx-auto", style:"width:30%;" %>
		<%end%>
		<div class="card-body">
		  	<h4 class="card-title"><%= @tweet.user.email %> </h4>
		    <p class="card-text"><%= @tweet.conteudo %></p>
		    <p class="card-text">(<%= @tweet.created_at.strftime("%d/%m/%Y - %H:%M") %>)</p>	

		    <%= link_to 'Voltar', tweets_path %> |

		    <% if @tweet.user == current_user%>
		    	<%= link_to 'Editar', edit_tweet_path(@tweet) %> |
		    	<%= link_to 'Deletar', @tweet, method: :delete, data: { confirm: "você tem certeza que deseja apagar?"} %> 
		    <%end%>


  		</div>
</center>


	
	



</div>